pub fn add(a: i32, b: i32) -> i32 {
    let x = a + 5;
    let y = b + 5;
    x + y
}
pub fn sub(a: i32, b: i32) -> i32 {
    let x = a - 5;
    let y = b - 5;
    x - y
}

pub fn multiply(x: i32, y: i32) -> i32 {
    x * y
}
// pub makes the function accesible outside the module
